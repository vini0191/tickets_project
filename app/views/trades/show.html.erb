<div class="container">
  <div class="row justify-content-center">
    <h1>CHECK OUT</h1>
  </div>
  <p>Buyer: <%= current_user.name %></p>
  <p>Seller: <%= @ticket.user.name %></p>
  <div class="row justify-content-between">
    <p><%= link_to "back to event", event_path(@ticket.event) %></p>
    <p><%= link_to "back to home", events_path %></p>
  </div>
</div>
